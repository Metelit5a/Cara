# Server
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
DEBUG=true

# Model
MODEL_WEIGHTS_PATH=model_service/checkpoints/acne_model_best.pth
CONFIDENCE_THRESHOLD=0.4

# Storage (JSON file storage for POC)
STORAGE_BACKEND=json
STORAGE_PATH=storage

# MongoDB (prepared but not connected)
# STORAGE_BACKEND=mongodb
# MONGODB_URI=mongodb://localhost:27017
# MONGODB_DB_NAME=cara

# CORS
CORS_ORIGINS=["http://localhost:3000"]
